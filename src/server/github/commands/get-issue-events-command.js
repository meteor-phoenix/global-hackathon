/**
 * Get the latest event for the given issue
 */
GetIssueEventsCommand = (function() {
  var handle = function (orgName, repoName, number, githubConnection) {
    // TODO
  }

  return {
    handle: handle
  }
});